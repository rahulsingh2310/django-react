import React from 'react';
import {Button} from 'reactstrap';

const Note = ({title}) => (
  <React.Fragment>
    <Button color="info">{title}</Button>
   
  	
  </React.Fragment>
)

export default Note;
